__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/
.pytest_cache/
.coverage
htmlcov/
.venv/
.env
.ipynb_checkpoints/
data/cache/
*.csv.gz
.DS_Store
.idea/
.vscode/

# Project spec (kept out of the public repo)
Guideline.pdf

# Interview-prep package (built separately, not part of the library).
# Ignore its contents (generated PDFs, build scripts, flashcards, scratch) but
# keep the code-review fixes log tracked so it reaches main on merge.
interview_prep/*
!interview_prep/CODE_REVIEW_FIXES.md
